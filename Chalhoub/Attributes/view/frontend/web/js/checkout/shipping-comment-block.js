define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'Chalhoub_Attributes/checkout/shipping-comment-block'
        }
    });
});
